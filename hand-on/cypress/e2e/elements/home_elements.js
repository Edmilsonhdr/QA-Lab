class HomeElements {
  btnHideCookie = () => {
    return "#cookie_stop"; // Seletor CSS do botão "Ocultar Cookies"
  };

  selectAccount = () => {
    return "Account"; // Texto visível do elemento "Account"
  };

  btnLogin = () => {
    return "Login"; // Texto visível do botão "Login"
  };
}

export default HomeElements;
